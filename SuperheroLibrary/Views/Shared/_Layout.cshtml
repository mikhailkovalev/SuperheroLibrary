<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>@ViewBag.Title</title>
    <link href="@Url.Content("~/Content/Site.css")" rel="stylesheet" type="text/css" />
</head>
<nav>
    <table>
        <tr>
            <td>
                @Html.ActionLink("Главная", "Index", "Home")
            </td>
            @if (User.Identity.IsAuthenticated)
            {
                <td>
                    @Html.ActionLink("Выйти", "Logoff", "Account")
                </td>
            }
            else
            {
                <td>
                    @Html.ActionLink("Войти", "Login", "Account")
                </td>
                <td>
                    @Html.ActionLink("Зарегистрироваться", "Register", "Account")
                </td>
            }
        </tr>
        @if (User.Identity.IsAuthenticated)
        {
            <tr>
                <!--Create-->
                <td>
                    @Html.ActionLink("Создать Героя", "Create", "Hero")
                </td>
                <td>
                    @Html.ActionLink("Создать Способность", "Create", "Ability")
                </td>
            </tr>
            <tr>
                <!--Show-->
                <td>
                    @Html.ActionLink("Перейти к списку героев", "Show", "Hero")
                </td>
                <td>
                    @Html.ActionLink("Перейти к списку способностей", "Show", "Ability")
                </td>
            </tr>
        }
    </table>
</nav>
<body>

    @RenderBody()
</body>
</html>